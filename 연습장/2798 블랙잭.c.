//<입력>
//1l. 카드개수N, 가깝게만들기M
//2l. N개의 수

//<출력>
//N개의 수 중에서 3개를 합해서 M에 최대한 가까운 합을 구하기.

#include <stdio.h>
#include <string.h>

int main() {
    int N, M;
    scanf("%d %d", &N, &M);                 //카드개수N, M에 가깝게만들기
    int arr[N] = { };
    
    for(int i=0; i<N; i++) {                //각 배열에 N개의 수 순서대로 대입
        scanf("%d", &arr[i]);
    }
    
    
}
